import 'package:flutter/material.dart';

class GoodsInTransit extends StatelessWidget {
  const GoodsInTransit({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Goods in transit')),
    );
  }
}
